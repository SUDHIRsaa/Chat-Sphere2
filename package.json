{
  "name": "new-folder",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "npm install --prefix backend && npm install  --prefix chatsphere-new && npm run build --prefix chatsphere-new ",
    "start": " npm run start --  --prefix backend"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "new-folder": "file:"
  }
}
